<?php

$myNumber = 50;
$rangeStart = 1;
$rangeEnd = 100;
if(($rangeStart <= $myNumber) && ($myNumber <= $rangeEnd)) {
    echo "My number is in the range";
} else {
    echo "My number is not in the range";
}
echo PHP_EOL;

